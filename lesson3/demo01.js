const users = ['Anurag', 'Akriti','Anup', 'Aniti','Shreya', 'Riya','Shelb', 'Marie'];

const getUserList = (users) => {
    users.forEach(user =>
        console.log(user)
        )
}


getUserList(users);